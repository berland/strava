/* style.css */
.fragment.fade-out.visible:not(.current-fragment) {
  display: none;
}

/*-- scss:mixins --*/

@mixin bodyBackground() {
  @include radial-gradient(rgba(252, 82, 0, 0.4), rgba(255, 255, 255, 1));
}
